import React,{useState}from'react';import'./AddMenuItem.css';import{api}from'../services/api';import{useNavigate}from'react-router-dom';
export default function AddMenuItem(){const navigate=useNavigate();const[form,setForm]=useState({name:'',description:'',category:'Mains',image_url:'',price:'',prep_station:'kitchen',available:true});const[error,setError]=useState('');
const change=e=>{const{name,value,type,checked}=e.target;setForm({...form,[name]:type==='checkbox'?checked:value})};
const submit=async e=>{e.preventDefault();setError('');try{await api.post('menu-items/',form);navigate('/menu-items')}catch(err){const d=err.response?.data;setError(d?Object.values(d).flat().join(' '):'Could not save this menu item.')}};
return <main className="add-menu-item"><div className="menu-form-head"><span>Menu studio</span><h1>Create a dish</h1><p>Give the kitchen what it needs and the guest something worth ordering.</p></div>{error&&<div className="alert alert-danger">{error}</div>}<form onSubmit={submit} className="form-container"><div className="field-wide"><label>Dish name *</label><input name="name" value={form.name} onChange={change} placeholder="e.g. Charred Lemon Chicken" required/></div><div><label>Category *</label><input name="category" value={form.category} onChange={change} placeholder="Mains, Drinks, Dessert..." required/></div><div><label>Price (KSh) *</label><input type="number" min="0" step="0.01" name="price" value={form.price} onChange={change} required/></div><div className="field-wide"><label>Description</label><textarea name="description" value={form.description} onChange={change} placeholder="Ingredients, flavour, accompaniments..."/></div><div className="field-wide"><label>Image URL</label><input name="image_url" value={form.image_url} onChange={change} placeholder="https://..."/></div><div><label>Preparation station</label><select name="prep_station" value={form.prep_station} onChange={change}><option value="kitchen">Kitchen</option><option value="grill">Grill</option><option value="bar">Bar</option><option value="dessert">Dessert</option></select></div><label className="availability"><input type="checkbox" name="available" checked={form.available} onChange={change}/> Available to order</label><div className="form-actions field-wide"><button type="button" className="cancel" onClick={()=>navigate('/menu-items')}>Cancel</button><button type="submit" className="save-dish">Save dish</button></div></form></main>}