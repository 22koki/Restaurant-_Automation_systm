import React,{useState}from'react';import{Navigate,useLocation,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import'./Login.css';
const home={owner:'/',manager:'/',cashier:'/pos',waiter:'/service',kitchen:'/kitchen',storekeeper:'/'};
export default function Login(){const{user,login}=useAuth();const nav=useNavigate(),loc=useLocation();const[username,setUsername]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to={home[user.role]||'/'} replace/>;
const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const u=await login(username,password);nav(loc.state?.from||home[u.role]||'/',{replace:true})}catch(err){setError(err.response?.data?.detail||'Could not sign in. Check your username and password.')}finally{setBusy(false)}};
return <main className="login-shell"><section className="login-brand"><span>Savour Restaurant OS</span><h1>Service starts here.</h1><p>One secure workspace for the floor, kitchen, till and management team.</p><div><b>01</b> Take orders <b>02</b> Run service <b>03</b> Close the day</div></section><section className="login-panel"><form onSubmit={submit}><span>Staff access</span><h2>Welcome back.</h2><p>Sign in with the account created for your restaurant role.</p><label>Username<input autoFocus value={username} onChange={e=>setUsername(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="login-error">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in to workspace'}</button><a href="/menu">Open customer menu instead →</a></form></section></main>}